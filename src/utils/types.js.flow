// @flow

import type { $Request } from 'express';

export type session$Request = $Request & {
    user: UserModel,
    logout: void => void,
};

export type UserModel = {
    id: string,
    googleId: string,
};

export type DoneCallback = (?Error, any) => any;
